/** @type {import('ts-jest/dist/types').Initial0ptionsTsJest} */
module.exports = {
    preset: "ts-jest",
    testEnvironment: "node",
    testMatch: ["**/**/*.test.ts"],
    verbose: true,
    forceExit: true,
    // clearMogks: true,
}