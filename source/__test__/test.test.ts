// import { dummyFun } from "../utilities";
import { dummyFun } from '../utilities'

const obj = { one: 1 }

test('Sum (1,2) should return 3', () => {
    expect(dummyFun(1, 2)).toBe(3)
})


test('Sum (1,2) should return 3', () => {
    expect(typeof obj).toBe('object');
})

